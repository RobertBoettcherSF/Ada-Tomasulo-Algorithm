project Tomasulo is
   for Source_Dirs use (".");
   for Object_Dir use ".";
   for Exec_Dir use ".";
   for Main use ("tests.adb");
   
   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-gnata"); -- Enable assertions
   end Compiler;
end Tomasulo;
